title = "Token Intelligence Gitleaks configuration"

[extend]
useDefault = true

[allowlist]
description = "Exact synthetic test fixture committed during release-candidate hardening; never used against WorkOS."
regexes = [
  '''sk_test_release_callback'''
]
