"use client";

import Link from "next/link";
import { useCallback, useEffect, useMemo, useRef, useState } from "react";
import { formatTokens } from "@/lib/format";
import { MODEL_CATALOG, PROVIDERS, modelsByProvider, type ModelCatalogEntry } from "@/lib/models";
import type { TokenMetrics } from "@/types/tokenizer";
import { ProviderCard } from "@/components/provider-card";

type InputMode = "text" | "words" | "tokens";
const EMPTY_METRICS: TokenMetrics = { requestId: 0, characters: 0, charactersWithoutSpaces: 0, words: 0, openaiExact: 0, anthropicEstimate: 0, geminiEstimate: 0, deepseekEstimate: 0, grokEstimate: 0, pieces: [] };
const OUTPUT_PRESETS = [{ label: "Classification", value: 5 }, { label: "RAG / Q&A", value: 15 }, { label: "Chat", value: 35 }, { label: "Full response", value: 60 }, { label: "Long generation", value: 100 }];
const toolLinks = [
  { href: "/tools/cost", eyebrow: "Cost Lab", title: "Compare every model", body: "Enter input, cache, and output once; rank cost across the catalog." },
  { href: "/tools/tokens-words", eyebrow: "Sizing", title: "Tokens ↔ words", body: "Translate context budgets into human-readable document scale." },
  { href: "/tools/memory", eyebrow: "Infrastructure", title: "GPU RAM planner", body: "Estimate model-weight VRAM from parameter count and precision." },
  { href: "/tools/speed", eyebrow: "Latency", title: "Speed simulator", body: "Preview time-to-first-token and streamed decode pacing." },
];
function estimatedTokensFromWords(words: number) { return Math.max(0, Math.ceil(words / 0.75)); }

export function TokenCalculator() {
  const [mode, setMode] = useState<InputMode>("text");
  const [text, setText] = useState("Design an agentic support workflow that summarizes a customer ticket, retrieves the account context, proposes the next best action, and drafts a concise response.");
  const [manualWords, setManualWords] = useState(500);
  const [manualTokens, setManualTokens] = useState(1000);
  const [metrics, setMetrics] = useState<TokenMetrics>(EMPTY_METRICS);
  const [outputPercent, setOutputPercent] = useState(35);
  const [cachedPercent, setCachedPercent] = useState(0);
  const [requestsPerMonth, setRequestsPerMonth] = useState(10_000);
  const [sortMode, setSortMode] = useState<"provider" | "cost">("provider");
  const workerRef = useRef<Worker | null>(null);
  const requestIdRef = useRef(0);
  useEffect(() => { const worker = new Worker(new URL("../workers/tokenizer.worker.ts", import.meta.url)); workerRef.current = worker; worker.onmessage = (event: MessageEvent<TokenMetrics>) => { if (event.data.requestId === requestIdRef.current) setMetrics(event.data); }; return () => worker.terminate(); }, []);
  useEffect(() => { if (mode !== "text" || !workerRef.current) return; const timer = window.setTimeout(() => { requestIdRef.current += 1; workerRef.current?.postMessage({ requestId: requestIdRef.current, text }); }, 80); return () => window.clearTimeout(timer); }, [text, mode]);
  const baseInputTokens = mode === "text" ? metrics.openaiExact : mode === "words" ? estimatedTokensFromWords(manualWords) : manualTokens;
  const outputTokens = Math.max(0, Math.round(baseInputTokens * (outputPercent / 100)));
  const inputTokensFor = useCallback((model: ModelCatalogEntry) => { if (mode !== "text") return baseInputTokens; switch (model.tokenizer) { case "openai-o200k": return metrics.openaiExact; case "anthropic-estimate": return metrics.anthropicEstimate; case "gemini-estimate": return metrics.geminiEstimate; case "deepseek-estimate": return metrics.deepseekEstimate; case "grok-estimate": return metrics.grokEstimate; default: return metrics.openaiExact; } }, [mode, baseInputTokens, metrics]);
  const providerOrder = useMemo(() => sortMode === "provider" ? PROVIDERS : [...PROVIDERS].sort((a, b) => { const aModel = modelsByProvider(a)[0]; const bModel = modelsByProvider(b)[0]; return (aModel.pricing.input + aModel.pricing.output * (outputPercent / 100)) - (bModel.pricing.input + bModel.pricing.output * (outputPercent / 100)); }), [sortMode, outputPercent]);
  const tokenPieces = metrics.pieces.slice(0, 120);
  return <main><section className="hero shell"><div className="hero__copy"><span className="pill">Private browser calculator</span><h1>Plan AI cost, context <span>and</span> capacity.</h1><p>Tokenize prompts locally, compare provider pricing, automatically account for long-context tiers, and move from a quick estimate into deeper cost, memory, and latency tools.</p><div className="hero-actions"><a href="#calculator" className="button button--primary">Open calculator</a><Link href="/models" className="button button--ghost">Browse {MODEL_CATALOG.length} models</Link></div></div><div className="hero__status"><span className="status-dot" />Pricing catalog verified 2026-08-30</div></section><section id="calculator" className="workspace shell" aria-label="Token calculator workspace"><div className="mode-tabs" role="tablist" aria-label="Input mode">{(["text", "words", "tokens"] as InputMode[]).map((item) => <button key={item} className={mode === item ? "mode-tab mode-tab--active" : "mode-tab"} type="button" onClick={() => setMode(item)}>{item === "text" ? "Text input" : item === "words" ? "Word count" : "Token count"}</button>)}</div><div className="input-panel">{mode === "text" && <><div className="input-panel__header"><label htmlFor="prompt">Prompt / context</label><button type="button" className="text-button" onClick={() => setText("")}>Clear text</button></div><textarea id="prompt" value={text} onChange={(event) => setText(event.target.value)} spellCheck={false} /></>}{mode === "words" && <label className="number-input-block" htmlFor="word-count"><span>Total words</span><input id="word-count" type="number" min="0" value={manualWords} onChange={(event) => setManualWords(Number(event.target.value) || 0)} /><small>Planning heuristic: roughly 0.75 English words per token.</small></label>}{mode === "tokens" && <label className="number-input-block" htmlFor="token-count"><span>Total input tokens</span><input id="token-count" type="number" min="0" value={manualTokens} onChange={(event) => setManualTokens(Number(event.target.value) || 0)} /><small>Use when provider telemetry already gives you a token count.</small></label>}<div className="metrics-strip metrics-strip--5"><div><span>Tokens</span><strong>{formatTokens(baseInputTokens)}</strong><small>{mode === "text" ? "o200k reference" : "planning input"}</small></div><div><span>Words</span><strong>{mode === "text" ? formatTokens(metrics.words) : mode === "words" ? formatTokens(manualWords) : "—"}</strong></div><div><span>No-space chars</span><strong>{mode === "text" ? formatTokens(metrics.charactersWithoutSpaces) : "—"}</strong></div><div><span>All chars</span><strong>{mode === "text" ? formatTokens(metrics.characters) : "—"}</strong></div><div><span>Output plan</span><strong>{formatTokens(outputTokens)}</strong><small>{outputPercent}% of input</small></div></div></div>{mode === "text" && tokenPieces.length > 0 && <details className="token-visualizer"><summary>Token visualization <span>first {tokenPieces.length} o200k pieces</span></summary><div className="token-cloud">{tokenPieces.map((piece, index) => <span key={`${piece.id}-${index}`} title={`Token ${piece.id}`} className={`token-chip token-chip--${index % 5}`}>{piece.text.replace(/ /g, "·").replace(/\n/g, "↵") || "∅"}</span>)}</div></details>}<div className="planner-grid"><div className="planner-card planner-card--wide"><div className="planner-card__heading"><div><p className="eyebrow">Output planning</p><h2>Expected response size</h2></div><strong>{outputPercent}%</strong></div><div className="preset-row">{OUTPUT_PRESETS.map((preset) => <button key={preset.value} type="button" className={outputPercent === preset.value ? "preset preset--active" : "preset"} onClick={() => setOutputPercent(preset.value)}>{preset.label}</button>)}</div><input aria-label="Output token percentage" className="range" type="range" min="0" max="150" step="5" value={outputPercent} onChange={(event) => setOutputPercent(Number(event.target.value))} /></div><div className="planner-card"><label htmlFor="cached-percent"><span className="eyebrow">Prompt caching</span><strong>Cached input</strong></label><div className="inline-number"><input id="cached-percent" type="number" min="0" max="100" value={cachedPercent} onChange={(event) => setCachedPercent(Math.min(100, Math.max(0, Number(event.target.value) || 0)))} /><span>%</span></div><small>Applied where a published cached-input rate exists.</small></div><div className="planner-card"><label htmlFor="monthly-requests"><span className="eyebrow">Budget forecast</span><strong>Requests / month</strong></label><input id="monthly-requests" type="number" min="0" value={requestsPerMonth} onChange={(event) => setRequestsPerMonth(Number(event.target.value) || 0)} /><small>Convert request economics into a monthly planning number.</small></div></div></section><section className="comparison shell"><div className="section-heading"><div><p className="eyebrow">Cost snapshot</p><h2>Compare by provider</h2><p>Published long-context tiers are applied automatically where represented in the catalog.</p></div><label className="sort-control">Sort <select value={sortMode} onChange={(event) => setSortMode(event.target.value as "provider" | "cost")}><option value="provider">Provider</option><option value="cost">Lowest base cost</option></select></label></div><div className="provider-grid">{providerOrder.map((provider) => <ProviderCard key={provider} provider={provider} models={modelsByProvider(provider)} inputTokensFor={inputTokensFor} outputTokens={outputTokens} cachedPercent={cachedPercent} requestsPerMonth={requestsPerMonth} />)}</div></section><section className="tools-section shell"><div className="section-heading"><div><p className="eyebrow">Planning suite</p><h2>Go beyond token counting.</h2><p>Dedicated tools use the same model and pricing foundation instead of duplicating logic.</p></div></div><div className="tool-link-grid">{toolLinks.map((tool) => <Link key={tool.href} href={tool.href} className="tool-link"><p className="eyebrow">{tool.eyebrow}</p><h3>{tool.title}</h3><p>{tool.body}</p><span>Open tool →</span></Link>)}</div></section><section className="trust shell"><div><p className="eyebrow">Privacy architecture</p><h2>The browser calculator remains local-first.</h2></div><div className="trust-grid"><div><strong>01</strong><h3>Local tokenization</h3><p>Text entered here goes to a Web Worker in this page, not a prompt-analysis backend.</p></div><div><strong>02</strong><h3>Honest precision</h3><p>o200k is a local OpenAI planning reference. Other provider families stay explicitly labeled as estimates.</p></div><div><strong>03</strong><h3>Explicit API boundary</h3><p>The optional developer API is a separate, deliberate server call with a documented privacy boundary and no-store responses.</p></div></div></section><footer className="footer shell"><span>Token Intelligence · clean-room implementation</span><span>{MODEL_CATALOG.length} model profiles · public tools free</span></footer></main>;
}
