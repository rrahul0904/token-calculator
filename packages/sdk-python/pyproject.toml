[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "token-intelligence"
version = "0.1.0"
description = "Python client for Token Intelligence agent economics APIs"
requires-python = ">=3.10"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["token_intelligence"]
